# PG CERBOS Environment Configuration
# Copy this file to .env and modify as needed

# PostgreSQL Configuration
PG_SUPERUSER=postgres
PG_SUPERPASS=postgres

# MinIO Configuration
MINIO_USER=minio
MINIO_PASS=minio123

# Application Configuration
SECRET_KEY=your-secret-key-change-in-production

# OpenAI (natural language to Cypher, chart suggestion, ECharts generation)
# Pass these in .env; docker compose loads them into policy-registry-backend.
# OPENAI_API_KEY=sk-...          # required for LLM features
# OPENAI_MODEL=gpt-4o-mini       # default for most LLM calls
# OPENAI_MODEL_CYPHER=gpt-4o-mini   # model for NL → Cypher generation
# OPENAI_MODEL_CHART=gpt-4o-mini    # model for chart type + ECharts option
# OPENAI_BASE_URL=               # optional, e.g. Azure endpoint

# Optional: Override default ports (uncomment if needed)
# POSTGRES_PORT=5434
# TRINO_COORDINATOR_PORT=8080
# POLICY_REGISTRY_PORT=8082
# POLICY_UI_PORT=8083
# CERBOS_PORT=3593
# MINIO_PORT=9000
# MINIO_CONSOLE_PORT=9001
# NESSIE_PORT=19120 