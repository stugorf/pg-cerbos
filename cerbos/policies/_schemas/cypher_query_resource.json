{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "title": "Cypher Query Resource Schema",
  "description": "Schema for Cypher query resources with metadata extracted from query parsing",
  "properties": {
    "query_type": {
      "type": "string",
      "enum": ["cypher", "gremlin", "sparql", "gql"],
      "description": "Type of graph query"
    },
    "query": {
      "type": "string",
      "description": "The full Cypher query string"
    },
    "analysis_version": {
      "type": "string",
      "description": "Version of the normalized graph query analysis contract"
    },
    "complete": {
      "type": "boolean",
      "description": "Whether the analyzer produced complete metadata for authorization"
    },
    "language": {
      "type": "string",
      "enum": ["cypher", "sparql", "gremlin", "gql"],
      "description": "Graph query language"
    },
    "dialect": {
      "type": "string",
      "description": "Language dialect or parser profile"
    },
    "mode": {
      "type": "string",
      "enum": ["read", "write"],
      "description": "Requested execution mode"
    },
    "statement_type": {
      "type": "string",
      "enum": ["read", "write"],
      "description": "Analyzer-classified statement type"
    },
    "is_read_only": {
      "type": "boolean",
      "description": "Whether the analyzer considers the query read-only"
    },
    "has_write_operation": {
      "type": "boolean",
      "description": "Whether the query contains graph mutation or write operations"
    },
    "accessed_node_labels": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Normalized node labels accessed by the query"
    },
    "accessed_edge_types": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Normalized relationship or edge types accessed by the query"
    },
    "accessed_properties": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Normalized properties referenced by the query"
    },
    "path_patterns": {
      "type": "array",
      "description": "Analyzer-produced path pattern summaries"
    },
    "max_traversal_depth": {
      "type": "integer",
      "minimum": 0,
      "description": "Normalized maximum traversal depth"
    },
    "has_variable_length_paths": {
      "type": "boolean",
      "description": "Whether the query contains variable-length path syntax"
    },
    "has_aggregation": {
      "type": "boolean",
      "description": "Whether the query contains aggregation"
    },
    "has_subquery": {
      "type": "boolean",
      "description": "Whether the query contains a subquery"
    },
    "has_union": {
      "type": "boolean",
      "description": "Whether the query contains UNION"
    },
    "has_optional_match": {
      "type": "boolean",
      "description": "Whether the query contains OPTIONAL MATCH or equivalent optional graph pattern"
    },
    "limit": {
      "type": ["integer", "null"],
      "minimum": 0,
      "description": "Parsed query LIMIT if present"
    },
    "estimated_result_bound": {
      "type": "integer",
      "minimum": 0,
      "description": "Best available result bound or estimate"
    },
    "filters": {
      "type": "object",
      "description": "Normalized filter values extracted from the query"
    },
    "parameters": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Named parameters referenced by the query"
    },
    "warnings": {
      "type": "array",
      "description": "Analyzer warnings, including security-relevant warnings"
    },
    "node_labels": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "List of node labels accessed in the query (e.g., Customer, Transaction, Account, SAR)"
    },
    "relationship_types": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "List of relationship types accessed in the query (e.g., OWNS, SENT_TXN, TO_ACCOUNT)"
    },
    "max_depth": {
      "type": "integer",
      "minimum": 0,
      "description": "Maximum traversal depth (number of hops) in the query"
    },
    "has_aggregations": {
      "type": "boolean",
      "description": "Whether the query contains aggregation functions"
    },
    "query_pattern": {
      "type": "string",
      "enum": ["simple", "path", "multi_match", "with_clause", "union"],
      "description": "Type of query pattern detected"
    },
    "path_variables": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "List of path variable names used in the query"
    },
    "has_where_clause": {
      "type": "boolean",
      "description": "Whether the query has a WHERE clause"
    },
    "has_order_by": {
      "type": "boolean",
      "description": "Whether the query has an ORDER BY clause"
    },
    "has_limit": {
      "type": "boolean",
      "description": "Whether the query has a LIMIT clause"
    },
    "estimated_nodes": {
      "type": "integer",
      "minimum": 0,
      "description": "Estimated number of nodes that might be returned"
    },
    "estimated_edges": {
      "type": "integer",
      "minimum": 0,
      "description": "Estimated number of edges that might be returned"
    },
    "risk_rating": {
      "type": "string",
      "description": "Risk rating extracted from query filters (e.g., 'high', 'medium', 'low')"
    },
    "transaction_amount": {
      "type": "number",
      "description": "Transaction amount extracted from query filters"
    },
    "transaction_amount_min": {
      "type": "number",
      "description": "Minimum transaction amount extracted from query filters"
    },
    "transaction_amount_max": {
      "type": "number",
      "description": "Maximum transaction amount extracted from query filters"
    },
    "pep_flag": {
      "type": "boolean",
      "description": "PEP (Politically Exposed Person) flag extracted from query filters"
    },
    "severity": {
      "type": "string",
      "description": "Severity level extracted from query filters"
    },
    "status": {
      "type": "string",
      "description": "Status extracted from query filters"
    },
    "customer_team": {
      "type": "string",
      "description": "Customer team extracted from query filters (Phase 3: ABAC)"
    },
    "customer_region": {
      "type": "string",
      "description": "Customer region extracted from query filters (Phase 3: ABAC)"
    }
  },
  "required": ["query_type", "query"]
}
